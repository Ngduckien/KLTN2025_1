<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Bo góc -->
    <corners android:radius="10dp"/>

    <!-- Màu nền -->
    <solid android:color="#FFFFFF"/>

    <!-- Viền -->
    <stroke
        android:width="2dp"
        android:color="@color/black"/> <!-- Màu xanh dương -->

    <!-- Padding (tùy chọn) -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="4dp"
        android:bottom="4dp"/>

</shape>